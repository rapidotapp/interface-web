import React from 'react'

import ActiveIcon from '~atoms/ActiveIcon'

import ThemeX from '../../components/theme'

export default { title: 'ActiveIcon' }

export const activeIcon = () => {
  return (
    <ThemeX>
      <ActiveIcon />
    </ThemeX>
  )
}
